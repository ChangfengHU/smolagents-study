# 新 UI 快速开始指南

## 🚀 启动服务

```bash
cd /Users/huchangfeng/smolagents-study

# 启动服务
.venv/bin/python examples/grok_wechat_server.py
```

访问：http://localhost:8765

---

## 👀 新UI界面预览

### 顶部
- 标题：✨ Grok WeChat Article Studio
- 状态徽章（右上）：显示当前状态（准备就绪、生成中、完成等）
- 流程进度条：4个步骤，实时更新

### 左侧（配置面板）
1. **灵感池**
   - 下拉菜单：选择预设
   - 文本框：输入创意简报
   - 数字输入：预设数量（1-10）
   - 按钮："🔄 换一批灵感"

2. **指定创意**
   - 文本框：输入创意描述
   - 按钮："✓ 完成创意"

3. **文章设定**
   - 主题、读者、语气（文本框）
   - 小节数、图片比例、清晰度（下拉选择）
   - 存储模式（下拉选择）
   - 复选框：联网补充事实
   - 视觉风格（文本框）

4. **操作**
   - 按钮："▶️ 开始生成"

### 右侧（内容区）

#### 标签页
- **任务结果**：显示生成完成后的结果
  - 4个指标：任务ID、状态、标题、图片数
  - 下载链接：HTML、Markdown、JSON

- **预设对比**：显示所有生成的预设
  - 并列卡片，可点击选择
  - 每张卡片显示：名称、主题、读者、语气

- **文章草稿**：显示生成的文章文字
  - 标题、副标题、摘要
  - 各小节的内容（包括钩子、段落、要点）
  - 统计信息：字数、小节数

- **运行日志**：实时显示执行日志
  - 黑底绿字的终端风格

#### 页面预览
- iframe 显示生成的 HTML

---

## 🎯 典型工作流

### 工作流1：使用现有预设

```
1. 打开 http://localhost:8765
2. 左侧"灵感池"下拉菜单 → 选择一个预设
3. 表单自动填入（或手动修改）
4. 点"▶️ 开始生成"
5. 实时看到进度条更新
6. 当Draft生成完后，自动切换到"文章草稿"标签页 → 看到文字
7. 最终完成后，"任务结果"标签页显示链接和指标
8. 下面的iframe自动预览生成的HTML
```

### 工作流2：生成多个预设试试

```
1. 左侧"灵感池" → 输入创意简报，比如"AI时代的教育创新"
2. 修改预设数量为 5
3. 点"🔄 换一批灵感"
4. 等待几秒 → 自动切换到"预设对比"标签页
5. 看到5个预设并列展示
6. 点击最满意的预设 → 自动选中（背景变绿）
7. 表单自动填入这个预设的数据
8. 点"▶️ 开始生成"
```

### 工作流3：检查文字后再生成

```
1. 配置好参数，点"开始生成"
2. 等待段时间，当Draft生成完时
3. 自动切换到"文章草稿"标签页
4. 看到完整的文章内容（标题、副标题、各小节）
5. 评估质量：
   - 如果满意：继续等，图片会自动生成
   - 如果不满意：修改配置或创意，重新生成（成本更低）
6. 最终完成后看到结果
```

---

## 📊 流程进度条说明

```
┌─────────────────────────────────────────────────────┐
│ ① 预设生成 (3.2s) → ② 文章草稿 (8.5s) → ③ 图片生成 (45.3s) → ④ 完成 │
└─────────────────────────────────────────────────────┘

每个步骤：
- 圆圈显示步骤号
- 正在进行的步骤：背景绿色
- 已完成的步骤：绿色背景
- 未开始的步骤：灰色背景
- 下面显示耗时（秒）
```

---

## 🔧 功能说明

### 预设操作

**加载预设**
```
GET /api/presets
→ 返回可用预设列表，自动填入左侧下拉菜单
```

**生成预设**
```
点"🔄 换一批灵感" →
POST /api/presets/generate?brief=xxx&count=5
→ 生成5个预设 → 显示在"预设对比"标签页
→ 用户点击选择 → 自动填入左侧表单
```

**完成预设**
```
点"✓ 完成创意" →
POST /api/presets/complete?idea=xxx
→ 返回补全后的预设 → 自动填入左侧表单
```

### 文章生成

**创建任务**
```
点"▶️ 开始生成" →
POST /api/articles {...配置...}
→ 返回 job_id
→ 开始轮询状态
```

**轮询状态**
```
GET /api/jobs/{job_id}
→ 每1秒轮询一次
→ 获取 status、draft、logs、result
→ 前端实时更新UI
```

**结果展示**
```
当 status = "succeeded" 时：
- 显示 result（标题、链接等）
- 显示预生成的 HTML iframe
- 显示下载链接
```

---

## 🧪 测试要点

### 基础功能
- [ ] 页面加载，显示已有预设
- [ ] 选择预设，表单自动填入
- [ ] 点"换一批灵感"，生成新预设并显示
- [ ] 点"完成创意"，补全预设并填入
- [ ] 点"开始生成"，创建任务

### 流程显示
- [ ] 进度条显示，从步骤1开始
- [ ] 轮询时实时更新状态
- [ ] 完成时进度条全部高亮
- [ ] 左上角状态徽章实时更新

### 标签页切换
- [ ] 点"预设对比"看到生成的预设
- [ ] 点"文章草稿"看到生成的文字
- [ ] 点"运行日志"看到实时日志
- [ ] 点"任务结果"看到最终结果

### 响应式
- [ ] 桌面端：左右两列布局
- [ ] 平板端：自动调整为合适宽度
- [ ] 手机端：单列布局

---

## ⚠️ 已知限制

1. **Draft预览依赖后端**
   - 前端写好了，但需要后端返回 draft 信息
   - 目前后端可能还没有返回 draft 字段
   - 需要改造 `/api/jobs/{job_id}` 的返回值

2. **预设卡片的选择**
   - 目前可以视觉上选中（背景变绿）
   - 但实际表单填入逻辑需要完善

3. **错误处理**
   - 简单的 alert 提示
   - 生产环境应该有更好的错误展示

4. **日志展示**
   - 现在直接显示字符串数组
   - 应该加时间戳和颜色区分日志级别

---

## 📞 常见问题

**Q: 为什么看不到Draft预览？**
A: 需要后端改造 `/api/jobs/{job_id}` 返回draft信息。现在后端可能还没有返回这个字段。

**Q: 为什么预设对比里只有一个预设？**
A: 这是正常的，因为初始加载时只有保存的预设。要看多个预设，需要点"换一批灵感"生成新的。

**Q: 如何重新生成？**
A: 点"开始生成"创建新任务即可。之前的任务不会被覆盖，可以通过任务ID查看历史。

**Q: 可以同时生成多个任务吗？**
A: 可以，每次生成都会创建新的任务ID。后端会串行执行（因为有全局锁），所以会按顺序生成。

---

## 🚀 下一步优化

1. **后端改造**：返回draft信息和进度
2. **预设选择**：点击预设自动填入表单
3. **草稿编辑**：允许用户直接编辑Draft文本
4. **多版本对比**：显示不同版本的Draft差异
5. **历史记录**：列出所有生成过的任务

---

祝使用愉快！如有问题，欢迎反馈。
